import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The dev badge sits exactly over the person in the bottom of the rail.
  devIndicators: false,
};

export default nextConfig;
